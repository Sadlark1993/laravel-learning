// ############## runs the laravel project ###################
php artisan serve

// ############## runs the react project in dev mode ###############
npm run dev

// ############## executs php laravel code ###################
php artisan tinker

// now I'm just gonna play arround with Eloquent library
App\Models\Job::create(['title' => 'Acme Director', 'salary' => '$1,000,000']);
App\Models\Job::all()
App\Models\Job::find(3);

$job = App\Models\Job::find(34);
echo $job->salary;
$job->delete(); // deletes the record

// ##################### ARTISAN ###############################

// rebuilds the database, deleting all data in it
php artisan migrate:fresh

// creating a model
artisan make:model Comment


php artisan help make:model
// creates the model and the migration
php artisan make:model Post -m
// after this, you can edit the migration and then, migrate
php artisan migrate

// using a factory
php artisan tinker
App\Models\Person::factory()->create();
App\Models\Person::factory(10)->create();

//making a factory
php artisan make:factory JobFactory


// seeding a model. It must have a seeder and a proper factory.
php artisan db:seed --class=PersonSeeder

// creating records with specific state
App\Models\User::factory()->unverified()->create();


// creating a model with migration and factory
 php artisan make:model Employer -mf

 // for the factories, if you want several models to be related to a single model (onte to many),
 // you can use the method recicle.

 //to attach a record to another:
php artisan tinker
$tag = App\Models\Tag::first();
$tag->jobs()->attach(7);

//re run the query
 $tag->jobs()->get();

// gets the array of the titles of the jobs
$tag->jobs()->get()->pluck('title');



// ################# Disabling lazy loading from db #################
// you can do it (if you want) by editing the file app/Providers/AppServiceProvider.php:

/**
    * Bootstrap any application services.
    */
public function boot(): void
{
    Model::preventLazyLoading();
}

// if you do it, when your code atempt to lazy load, it will throw an error.
